<h1>Newsfeed</h1>
<hr />
<%= render 'form', post: @post %>

<%= render 'posts', posts: @posts%>

<div class="pagination" id="load-more-styling">
    <%= link_to_next_page(@posts, 'Load More', remote: true,
      class: "btn btn-primary btn-lg") %>
</div>
